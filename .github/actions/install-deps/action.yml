name: Install npm dependencies

description: Install npm dependencies, including lockfile-selected optional platform packages.

runs:
  using: composite
  steps:
    - name: npm ci
      shell: bash
      # npm selects the lockfile's optional package for the runner platform.
      run: npm ci --include=optional
